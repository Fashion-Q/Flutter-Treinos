      //gameRef.lighting?.animateToColor(Colors.black.withOpacity(0.5),);
      // gameRef.colorFilter?.animateTo(
      //   Colors.blue,
      //   blendMode: BlendMode.darken,
      //   duration: const Duration(milliseconds: 3500),
      // );
      // if (camera) {
      //   camera = !camera;
      //   gameRef.camera.moveToTargetAnimated(gameRef.visibleEnemies().first);
      // } else {
      //   camera = !camera;
      //   gameRef.camera.moveToPlayerAnimated();
      // }